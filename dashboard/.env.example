# ─── 健康存摺儀表板 — 前端環境變數 ────────────────────────────────────────────
# 複製此檔案為 .env.local（不會被 git 追蹤）
# cp dashboard/.env.example dashboard/.env.local

# ── Google SSO（選填）────────────────────────────────────────────────────────

# Google OAuth 2.0 用戶端 ID（須與 server/.env 的 GOOGLE_CLIENT_ID 相同）
# 不填則隱藏登入頁的 Google 按鈕
# VITE_GOOGLE_CLIENT_ID=
