# 軟體需求規格書（SRS）— 健康存摺儀表板

## 1. 簡介

### 1.1 目的
本文件描述「健康存摺儀表板」（Health Passbook Dashboard）之功能與非功能需求，作為開發、版本追蹤與驗收的依據。

### 1.2 範圍
系統讓使用者匯入從衛福部「健保快易通」App 匯出的個人健康資料 JSON 檔案，於本機部署的網頁儀表板中瀏覽、篩選與追蹤自己的門診、住院、牙科、檢驗、疫苗接種與健檢資料。系統支援多使用者帳號、Google 登入、管理員後台，並將所有健康資料加密儲存於伺服器端。

### 1.3 名詞定義
| 名詞 | 說明 |
|------|------|
| NHI | National Health Insurance，台灣全民健康保險 |
| 健保快易通 | 衛福部提供之行動 App，可匯出個人健保就醫紀錄 JSON |
| 重點摘要 | 使用者釘選之檢驗項目／用藥的最新數值與趨勢摘要卡片 |
| 偏好設定（Preferences） | 使用者個人化設定，伺服器端持久化並跨裝置同步 |

## 2. 系統概觀

- **前端**：React + Vite + Tailwind CSS（`dashboard/`）
- **後端**：Node.js + Express（`server/`）
- **資料儲存**：伺服器本機檔案系統，AES-256-GCM 加密（`server/data/`）
- **部署**：Docker 多階段建置，可部署於 Zeabur 或自架 Docker host

## 3. 使用者角色

| 角色 | 權限 |
|------|------|
| 一般使用者 | 註冊／登入（帳密或 Google）、上傳並瀏覽自己的健康資料、管理個人帳號與偏好設定 |
| 管理員 | 一般使用者權限 + 管理所有使用者帳號、檢視登入紀錄。系統第一位註冊的帳號自動成為管理員 |

## 4. 功能需求

### 4.1 帳號與登入
- FR-1 使用者可透過帳號密碼註冊、登入
- FR-2 使用者可透過 Google OAuth 登入或將 Google 帳號連結至現有帳號
- FR-3 系統記錄每次登入的時間與來源 IP／國家
- FR-4 使用者可修改顯示名稱、密碼、主題模式（淺色／深色／跟隨系統）

### 4.2 資料匯入
- FR-5 使用者可上傳健保快易通匯出的 JSON 檔案
- FR-6 系統解析並正規化門診、住院、牙科、檢驗、疫苗接種、健檢等資料類型

### 4.3 儀表板瀏覽
- FR-7 儀表板以分頁（門診／檢驗／住院／牙科／預防保健）呈現各類資料
- FR-8 門診分頁提供申報項目（藥品／處置）長條圖，可依日期區間篩選
- FR-9 檢驗分頁提供各檢驗項目歷史趨勢圖，含正常參考範圍標示與異常次數統計
- FR-10 使用者可於檢驗趨勢卡片上釘選／取消釘選項目至重點摘要

### 4.4 重點摘要
- FR-11 首頁顯示「重點摘要」區塊，列出使用者釘選之檢驗項目與用藥的最新數值、與前次數值的差異（趨勢箭頭）、以及是否轉差之警示

### 4.5 個人化偏好設定
- FR-12 使用者可於帳號設定頁設定申報項目圖表的預設日期範圍（全部／近 3 個月／近半年／近 1 年／近 3 年）
- FR-13 使用者可自訂首頁顯示區塊，包括隱藏重點摘要、統計總覽，或個別分頁
- FR-14 系統記住使用者最後檢視的分頁，下次登入自動還原
- FR-15 所有偏好設定（釘選項目、隱藏區塊、預設日期範圍、最後分頁、已讀警示）儲存於伺服器端，跨裝置／瀏覽器同步；伺服器端驗證並清理輸入內容（去重、長度上限）

### 4.6 管理員後台
- FR-16 管理員可檢視所有使用者帳號、登入紀錄
- FR-17 管理員可刪除使用者帳號或登入紀錄

## 5. 非功能需求

- NFR-1 **安全性**：所有健康資料與帳號資料以 AES-256-GCM 加密儲存；主金鑰以 HKDF 派生；正式環境須設定 `JWT_SECRET`；CORS 來源可透過環境變數限制
- NFR-2 **可用性**：介面支援深色模式；主要互動元件符合基本無障礙標記（aria-label／aria-pressed／role）
- NFR-3 **在地化**：介面文字為繁體中文（台灣），資料格式對應台灣健保資料標準
- NFR-4 **可移植性**：可透過 Docker 部署於任意支援容器的平台

## 6. 資料模型（節錄）

- `User`：帳號、密碼雜湊或 Google ID、顯示名稱、主題模式、`preferences`
- `UserPreferences`：`pinnedLabItems`、`pinnedMedications`、`hiddenSections`、`defaultDateRange`、`lastActiveTab`、`acknowledgedAlerts`
- `NHIData`：`visits`、`labResults`、`hospitalizations`、`dentalVisits`、`vaccinations`、`checkupReports`（由前端解析器 `parsers/nhi-parser.ts` 產生）

## 7. 系統架構

```
dashboard/   前端 React + Vite + Tailwind（SPA）
server/      後端 Express API
  ├─ routes/auth.ts       登入、註冊、Google SSO
  ├─ routes/account.ts    個人帳號與偏好設定
  ├─ routes/data.ts       健康資料上傳與查詢
  ├─ routes/admin.ts      管理員功能
  └─ store.ts             加密資料存取層
```

## 8. 版本管理

### 8.1 版號規則
- **大版本（X.0.0）**：新增重大模組或架構調整
- **小版本（X.Y.0）**：新增功能或重要改進
- **修正版（X.Y.Z）**：Bug 修正

### 8.2 版本歷程

| 版本 | 日期 | 說明 |
|------|------|------|
| 1.4.1 | 2026-07-28 | 修復 shell-quote 相依套件高風險 DoS 安全性警告 |
| 1.4.0 | 2026-07-11 | 升級至 TypeScript 7 與 Tailwind CSS 4，相依套件全面更新 |
| 1.3.0 | 2026-07-07 | 既有明文健康資料可於前端一鍵加密遷移至用戶端加密格式 |
| 1.2.0 | 2026-07-06 | 健康資料改為用戶端加密儲存，並強化密碼原則與登入速率限制 |
| 1.1.1 | 2026-07-06 | 修復 Dependabot 安全性警告並升級相依套件（含 Express 5 相容性修正） |
| 1.1.0 | 2026-07-06 | 新增重點摘要區塊與儀表板個人化偏好設定（伺服器端同步） |
| 1.0.0 | — | 基準版本：多使用者帳號、Google 登入、NHI 資料匯入與儀表板瀏覽、管理員後台、加密儲存 |
